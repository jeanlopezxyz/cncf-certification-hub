# Example environment variables for local tooling (do not commit real secrets)
GITHUB_TOKEN=
SNYK_TOKEN=
SONAR_TOKEN=

# Usage:
#   cp .secrets.example .secrets
#   export $(grep -v '^#' .secrets | xargs)
