---
import Layout from '../../layouts/Layout.astro';
import StudyTipsGuide from '../../components/study-tips/StudyTipsGuide';
import { getLangFromUrl } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
---

<Layout title="Exam Preparation Tips" showSidebar={true}>
  <StudyTipsGuide client:load tipId="exam-preparation" lang={lang} />
</Layout>
